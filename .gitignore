# 빌드 산출물 — 게임 DLL을 참조해 빌드하므로 로컬 환경에서 재생성된다
bin/
obj/

# 개발 전용 — 게임 다중 실행용(내용은 app id). 게임 폴더에 두는 물건이라 여기 들어올 일은
# 없지만 실수로 복사해 커밋하는 것을 막는다. 최종 사용자에게 배포해서는 안 된다.
steam_appid.txt

# IDE / OS
.vs/
.idea/
*.user
*.suo
Thumbs.db
desktop.ini
